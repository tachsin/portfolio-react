import React from 'react';
import '../App.css'; 

const GradientPage = ({ className }) => {
  return (
    <div className={`gradient-page${className}`}>
    </div>
  );
};

export default GradientPage;